digraph {
  graph [rankdir=LR];

  Images -> Device;
  Images -> Pass;

  Pass -> Device;
  Pass -> Shaders;
  Pass -> Uniforms;

  CommandBuffer -> Images;
  CommandBuffer -> Device;
  CommandBuffer -> Pass;
}
